# vim: ft=make: sw=2:

${call core.show-current-location}

define util.join
  ${addsuffix $(2),$(1)}
endef

define util.set-variable
  ${call core.info,ruby,Setting variable $(1) to $(2)}
  override $(1) = $(2)
endef
